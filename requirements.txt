# TAL Pathway Explorer — runtime dependencies
# =============================================
# Install into a FRESH Python 3.13 environment (conda or venv):
#
#     pip install -r requirements.txt
#     streamlit run streamlit_app.py
#
# pip pulls the transitive deps (numpy, etc.) automatically. rdkit and
# doranet are the heavy scientific packages; on a fresh machine the
# install can take a few minutes.

# --- core app ---
streamlit==1.51.0
doranet==0.5.7a1
rdkit==2026.3.3
networkx==3.5
pandas==2.3.3
pyvis==0.3.2

# --- API backend (FastAPI "waiter" for the React frontend) ---
fastapi==0.139.0
uvicorn[standard]==0.51.0

# --- optional: biochemistry ΔG'° ranking (only if you enable the
#     "equilibrator" thermodynamics option in the UI) ---
equilibrator-api==0.6.0

# NOTE: the RMG thermodynamics option is a separate advanced feature that
# needs its own Python 2.7 environment (rmg_env) and is NOT installed by
# this file. Leave RMG disabled in the UI unless that env is set up.
#
# NOTE: the DORA-XGB feasibility criterion (optional, bio ranking) ALSO
# needs its own environment — its deps (xgboost 1.6.2, mordred) won't
# install on Python 3.13. Set it up once with:
#     conda create -n dora_xgb python=3.9 -y
#     conda run -n dora_xgb pip install DORA-XGB
# The main app talks to it via a subprocess bridge (src/dora_xgb_client.py);
# leave "Feasibility (DORA-XGB)" off in the UI unless that env exists.
